import {INPUT_FORM} from './types';



export const formInputs = (inputs)=> dispatch =>{
  let formInputs = [];
  if(inputs && inputs.length);
  inputs.forEach((val, ind)=>{
    formInputs.push()
  })


// export const jsonChange = () => dispatch => {
 return dispatch ({
  type: INPUT_FORM,
    payload: {inputs:[]}

  })
 }